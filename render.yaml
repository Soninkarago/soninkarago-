services:
  - type: web
    name: soninkarago
    runtime: python
    plan: free
    startCommand: python server.py
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PAYTECH_ENV
        value: prod
